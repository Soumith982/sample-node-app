name: Docker Image CI

on:
  push:
    branches: [ "Soumith982-patch-1" ]

jobs:

  build:

    runs-on: [self-hosted KS-2]

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag sample-nodejs-app-v1:$(date +%s)
