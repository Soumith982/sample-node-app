name: Docker Image CI

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:
jobs:

  build:

    runs-on: [self-hosted, linux, x64]

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: cd sample-node-app && docker build -t sample-node-app .
